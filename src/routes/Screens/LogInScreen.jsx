import { Button, View } from "react-native";


export default function LoginScreen({navigation}) {
    <View>TELA DE LOGIN</View>
    return (
        <Button
        title="clique aqui"
        onPress={() => navigation.navigate('Main')}
        />
    )
}